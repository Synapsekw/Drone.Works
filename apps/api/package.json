{
  "name": "@drone-works/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "auth:schema": "corepack pnpm run build:test-dependencies && corepack pnpm run build && node scripts/generate-auth-migration.mjs",
    "build": "tsc -p tsconfig.json",
    "build:test-dependencies": "corepack pnpm --filter @drone-works/contracts --filter @drone-works/parser --filter @drone-works/domain --filter @drone-works/telemetry --filter @drone-works/database --filter @drone-works/jobs run build",
    "start": "node dist/server.js",
    "test": "corepack pnpm run build:test-dependencies && vitest run test/contract.test.ts",
    "test:auth": "corepack pnpm run build:test-dependencies && corepack pnpm run build && node scripts/run-auth-tests.mjs",
    "test:authorization": "corepack pnpm run build:test-dependencies && corepack pnpm run build && node scripts/run-authorization-tests.mjs",
    "test:contract": "corepack pnpm run build:test-dependencies && vitest run test/contract.test.ts",
    "test:flight-api": "corepack pnpm run build:test-dependencies && corepack pnpm run build && node scripts/run-flight-api-tests.mjs",
    "test:upload": "corepack pnpm run build:test-dependencies && corepack pnpm run build && node scripts/run-upload-tests.mjs",
    "test:jobs": "corepack pnpm run build:test-dependencies && corepack pnpm run build && node scripts/run-jobs-tests.mjs",
    "test:normalize": "corepack pnpm run build:test-dependencies && node scripts/run-normalization-tests.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@drone-works/config": "workspace:*",
    "@drone-works/contracts": "workspace:*",
    "@drone-works/database": "workspace:*",
    "@fastify/swagger": "9.8.1",
    "@fastify/type-provider-typebox": "5.2.0",
    "@sinclair/typebox": "catalog:",
    "better-auth": "1.6.23",
    "fastify": "5.10.0"
  },
  "devDependencies": {
    "@drone-works/jobs": "workspace:*",
    "@drone-works/parser": "workspace:*",
    "@drone-works/telemetry": "workspace:*",
    "@types/node": "catalog:",
    "@types/pg": "8.15.5",
    "pg": "8.16.3",
    "pg-boss": "12.26.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
