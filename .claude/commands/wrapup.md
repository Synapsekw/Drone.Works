# /wrapup

Read `.agents/skills/wrapup/SKILL.md` in full and follow its Drone.Works wrap-up workflow exactly.

This invocation authorizes the vault-only session and north-star commit described by that protocol. It does not authorize staging source files, `.obsidian/` state, private fixtures, or pushing to a remote.
